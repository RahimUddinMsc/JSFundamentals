/*
    The HTML has section called 'Event Click' which is just a basic div with a heading
    Create some code which attaches the 'clickEvent' in the helpers.js file so it will trigger when it is clicked
    (Do not change the HTML)
*/

document.getElementById('event-click').addEventListener('click', () => clickEvent("test"))

